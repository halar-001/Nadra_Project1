package com.aidatabaseassistant.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ApplicationConfig {
    // Add common application beans here, e.g., RestTemplate, PasswordEncoder
}
